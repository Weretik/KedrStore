# Phase 02 — Infrastructure

- [x] T009 Add the agreed typed presentation-options model in `src/Catalog/Catalog.Application/Integrations/OneC/Options/` and bind it in `src/Catalog/Catalog.Infrastructure/DependencyInjection/CatalogIntegrationOneCExtension.cs` or the existing composition point. **Dependencies:** T002, T008.
- [x] T010 Add the approved identical configuration records to `src/Bootstrapper/Host.Api/appsettings.json` and `src/Bootstrapper/Host.Jobs/Host.Jobs/appsettings.json`. **Dependencies:** T009.
- [x] T011 Configure fields and any agreed ordering index in `src/Catalog/Catalog.Infrastructure/Configurations/ProductCategoryConfiguration.cs`; create a new migration in `src/Catalog/Catalog.Infrastructure/Migrations/`. **Dependencies:** T005, T006, T002.
- [x] T012 [P] Add persistence/migration coverage in `tests/IntegrationTests/` using the project test convention. **Dependencies:** T011.

## Verification (2026-07-29)

- `dotnet ef migrations add AddCategoryPresentationMetadata --project src/Catalog/Catalog.Infrastructure/Catalog.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context CatalogDbContext --output-dir Migrations` — completed successfully; migration `20260729060741_AddCategoryPresentationMetadata` was generated by EF Core.
- Both host configurations contain identical 55 presentation records, have no duplicate `(ProductTypeIdOneC, CategoryId)` keys, and cover every configured manual group and child category.
- `dotnet test tests/IntegrationTests/IntegrationTests.csproj --filter FullyQualifiedName~CategoryPresentationPersistenceTests` — passed: 2/2.
- The migration adds required columns and a `(ParentId, SortOrder, Id)` tree-ordering index. Its `Up` backfills existing rows with `Name` for both short names, `Int32.MaxValue` sort order, and `nlevel(Path) - 1`; `Down` drops the added index and columns.

## Checkpoint

The migration has a documented backfill and rollback path, hosts use the same approved configuration, and no applied migration is edited.
