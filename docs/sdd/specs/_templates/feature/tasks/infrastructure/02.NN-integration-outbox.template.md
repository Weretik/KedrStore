# Template of subphase 02.NN — Infrastructure: integration or outbox

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Infrastructure

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] TNN1 Define `<integration event/external adapter>`: contract, direction, retry/idempotency, failure behavior and owner.
- [ ] TNN2 Implement adapter or outbox persistence in `<exact path>` via Application contract.
- [ ] TNN3 Ensure the atomicity of the business change and outbox message record if the script requires it.
- [ ] TNN4 [P] Add integration tests: success, transient failure/retry, duplicate/idempotency and serialization contract.

## Checkpoint

The external integration is not called directly from the Domain and has a consistent failure behavior.
