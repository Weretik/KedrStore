# Template of subphase 02.NN — Infrastructure: read model

- [ ] TNN1 Implement `<ReadRepository/QueryService>` in `<exact path>` for agreed read contracts.
- [ ] TNN2 Add no-tracking projection, allowed filters/sorting/paging and only necessary joins/includes.
- [ ] TNN3 Check query plan or indexes for potentially heavy query if required by data volume/SLA.
- [ ] TNN4 [P] Add integration tests: filtering, sorting, pagination, empty result and data bounds.

## Checkpoint

Read model is optimized for agreed query and does not load aggregate unnecessarily.
