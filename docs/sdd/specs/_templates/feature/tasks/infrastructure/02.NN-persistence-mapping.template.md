# Template of subphase 02.NN — Infrastructure: persistence mapping

- [ ] TNN1 Create or modify EF Core configuration for `<Entity/Aggregate>` in `<exact path>` according to `data-model.md`.
- [ ] TNN2 Add only required keys, foreign keys, required/length constraints, unique constraints, indexes and concurrency token.
- [ ] TNN3 Configure mapping value objects, owned types, private collections and conversions according to Domain model rules.
- [ ] TNN4 [P] Add mapping and database constraints integration tests on the test database of the same type as production.

## Checkpoint

Persistence mapping does not leak into the Domain; database constraints support critical invariants.
