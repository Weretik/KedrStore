# Template of subphase 02.NN — Infrastructure: tests

- [ ] TNN1 Add missing integration tests for changed persistence mappings, migrations, read model and adapters to `<exact path of tests>`.
- [ ] TNN2 Use an isolated test database/container compatible with the production database if the behavior depends on the SQL provider.
- [ ] TNN3 Run the target Infrastructure tests and record the result.

## Checkpoint

Critical Infrastructure behavior is tested outside of unit-test doubles.
