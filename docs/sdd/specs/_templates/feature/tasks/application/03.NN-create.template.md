# Template of subphase 03.NN — Application: creation

- [ ] T028 Create `<Create…Command>` and result DTO in `<exact path>`.
- [ ] T029 Create FluentValidation validator: mandatory fields, format, boundaries, interfield rules and dependency checks through Application abstractions.
- [ ] T030 Implement handler: create aggregate only through Domain behavior.
- [ ] T031 Save changes via write repository and `IUnitOfWork` by passing `CancellationToken`.
- [ ] T032 Explicitly return the agreed `Ardalis.Result`: success, invalid, conflict/forbidden (if necessary) and error.
- [ ] T033 [P] Add validator tests and handler tests: success, validation failure, conflict/forbidden (if needed) and error case.

## Checkpoint

The creation command does not bypass Domain rules, does not record data after validation failure, has clear results and tests.
