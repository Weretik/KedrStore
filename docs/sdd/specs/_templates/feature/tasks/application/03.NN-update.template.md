# Template of subphase 03.NN — Application: editing

- [ ] T031 Create `<Update…Command>` and result DTO in `<exact path>`.
- [ ] T032 Create FluentValidation validator: mandatory fields, format, boundaries, interfield rules and dependency checks through Application abstractions.
- [ ] T033 Implement aggregate loading via write abstraction and `not found' script.
- [ ] T034 Apply allowed changes via Domain behavior and save them via `IUnitOfWork` with `CancellationToken`.
- [ ] T035 Implement agreed conflict scenarios, if they exist.
- [ ] T036 Explicitly return the agreed `Ardalis.Result`: success, invalid, not found, conflict/forbidden (if needed) and error.
- [ ] T037 [P] Add validator tests and handler tests: success, validation failure, not found, conflict/forbidden (if needed) and error case.

## Checkpoint

The command changes only allowed fields, does not write data after validation failure, has clear results and tests.
