# Template of subphase 03.NN — Application: obtaining one resource

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Application

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] T025 Create `<Get…ByIdQuery>` and response DTO in `<exact path>`.
- [ ] T026 Add projection/specification for obtaining one resource through read abstraction.
- [ ] T027 Implement handler and explicit `not found' result when resource is missing.
- [ ] T028 Add query validator only if necessary: ​​identifier format and limits.
- [ ] T029 Explicitly return the agreed `Ardalis.Result`: success, invalid identifier, not found and error.
- [ ] T030 [P] Add handler and, if available, validator tests: success, invalid identifier, not found and error case.
- [ ] T031 Check transmission of `CancellationToken` and participation of query in Mediator pipeline.

## Checkpoint

The query returns only the required DTO, does not mask the missing resource as success, and has the required validation, explicit results, and tests.
