# Template of subphase 03.NN — Application: other use cases

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Application

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] T039 Create separate request and DTO for `<use case name>` in `<exact path>`.
- [ ] T040 Add projection/specification for read use case; for write use case — Domain behavior, write abstraction and `IUnitOfWork`.
- [ ] T041 Create a validator only if the use case has input rules or dependency checks.
- [ ] T042 Implement handler and explicit agreed `Ardalis.Result` for `<use case name>`.
- [ ] T043 [P] Add validator tests (if available) and handler tests: success, invalid/not found/conflict/error — only relevant scenarios.
- [ ] T044 Check transmission of `CancellationToken` and use case participation in Mediator pipeline.

## Checkpoint

Each additional script is implemented as a complete vertical CQRS slice: contract, validation as needed, handler, result, and tests.
