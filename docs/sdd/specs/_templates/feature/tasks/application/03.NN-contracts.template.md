# Template of subphase 03.NN — Application: contracts and data access

- [ ] T016 Check available Application contracts, DTO and mapping helpers in the module; reuse them if they match the use case.
- [ ] T017 Create or update only required Application contracts: commands, requests and service/repository interfaces in `<exact paths>`.
- [ ] T018 Create separate request/response DTOs for each CQRS operation only if the existing Application contract does not cover the required data form; do not confuse them with HTTP DTOs and Domain entities.
- [ ] T019 Define mapping between DTO, Domain model and read projection; create a feature mapper/helper only for repetitive or non-trivial mapping, otherwise leave the mapping local and explicit in the handler.
- [ ] T020 Add required Application abstractions for data: write repository/unit of work for commands and read repository/query service for reading.
- [ ] T021 Do not add a direct dependency of Application on DbContext, if this is not agreed by the architecture.

## Checkpoint

Each use case has its own contracts and uses an agreed data path.
