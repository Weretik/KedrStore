# Template of subphase 03.NN — Application: deletion

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Application

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] T035 Create `<Delete…Command>` in `<exact path>`.
- [ ] T036 Implement resource loading and `not found' script.
- [ ] T037 Execute agreed hard delete or soft delete through Domain behavior.
- [ ] T038 Save change via write repository and `IUnitOfWork` by passing `CancellationToken`.
- [ ] T039 Explicitly return the agreed `Ardalis.Result`: success, not found, conflict/forbidden (if necessary) and error.
- [ ] T040 [P] Add handler tests: success, not found, conflict/forbidden (if needed) and error case.

## Checkpoint

The deletion type meets the requirements; the missing resource and dependent data are processed explicitly; results and tests are ready.
