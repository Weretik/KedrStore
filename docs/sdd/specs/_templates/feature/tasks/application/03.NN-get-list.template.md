# Template of subphase 03.NN — Application: getting a list

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Application

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Work

- [ ] T021 Create `<Get…ListQuery>` and list element DTO at `<exact path>`.
- [ ] T022 Define and implement allowed filters, sorting, pagination, and list response metadata.
- [ ] T023 Add no-tracking read projection/specification via agreed read abstraction.
- [ ] T024 Implement a handler that returns a list of DTOs; an empty list is a successful result.
- [ ] T025 Add query validator only if necessary: ​​valid filters, sort field, page/page size and identifier format.
- [ ] T026 Explicitly return the agreed `Ardalis.Result`: success with empty or filled list, invalid parameters and error.
- [ ] T027 [P] Add handler and, if available, validator tests: filled list, empty list, invalid parameters and error case.
- [ ] T028 Check transmission of `CancellationToken` and participation of query in Mediator pipeline.

## Checkpoint

The list query does not load redundant data, correctly handles an empty result, has the required validation, explicit results and tests.
