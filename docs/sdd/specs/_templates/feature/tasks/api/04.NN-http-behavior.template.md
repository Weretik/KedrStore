# Subphase template 04.NN — API: HTTP behavior

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** API

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Work

- [ ] T060 Check available API contracts and mapping helpers in `<Module>.Api/Contracts`; use them if they match the endpoint.
- [ ] T061 Create API request/response DTO and mapper only if HTTP binding or public API contract differs from Application DTO; do not use Application DTO as an HTTP contract without an explicit solution.
- [ ] T062 Check authentication/authorization configuration in composition root; add policy, role mapping or permission registration only if the required rule does not exist yet.
- [ ] T063 Apply the agreed authorization policy/role to each endpoint and check that no broken object/function level authorization occurs.
- [ ] T064 Use existing `ToActionResult` for `Ardalis.Result`; not create a manual Result-to-HTTP mapping for the endpoint, except as an agreed exception.
- [ ] T065 Determine for the list endpoint a successful HTTP response with an empty array and pagination metadata, if applicable.
- [ ] T066 Add response metadata: status codes, response body types, `ProducesResponseType' or equivalent project mechanism.

## Checkpoint

The HTTP behavior of all endpoints is consistent with each other and does not duplicate business rules from the Application.
