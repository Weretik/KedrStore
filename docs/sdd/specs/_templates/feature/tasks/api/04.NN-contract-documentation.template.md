# Template of subphase 04.NN — API: OpenAPI documentation

- **Task ID:** EN-<NNN>
- **Enables:** <SC-IDs>
- **Depends on:** <task IDs or none>
- **Exact paths:** <human-readable and OpenAPI contract paths>
- **Test level:** API contract

## Contract evidence

- Contract validation command and result:
- Aggregate reference check:
- Consumer or owner decision:

## Work

- [ ] T065 Update `docs/sdd/specs/<module>/<NNN>-<feature>/contracts/api-contract.md` for each endpoint: path, HTTP method, request, response, status codes, authorization and examples.
- [ ] T066 Create or update versioned OpenAPI contract `docs/sdd/contracts/<module>/<feature>.openapi.yaml` for `<Get…ListQuery>` and `<Get…ByIdQuery>`.
- [ ] T067 Add operations create, update, delete and every other agreed use case to the OpenAPI contract.
- [ ] T068 Describe in OpenAPI all schemes, parameters, filters, sorting, pagination, security requirements and success/error responses.
- [ ] T069 Before transport implementation, validate the versioned contract and
      confirm that `docs/sdd/contracts/openapi.yaml` contains the correct `$ref`.

## Checkpoint

Each agreed operation is described and validated in versioned OpenAPI YAML
before transport implementation when practical.
