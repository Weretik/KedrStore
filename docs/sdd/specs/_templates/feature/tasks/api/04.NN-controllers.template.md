# Template of subphase 04.NN — API: controllers and endpoint methods

- [ ] T050 Check the existing API pattern in the module: `Controllers/`, `Contracts/`, `ISender.Send`, `BuildingBlocks.Api` and extension `ToActionResult` for `Ardalis.Result`.
- [ ] T051 Check composition root in `<exact path of Program.cs/extension>`: whether controller infrastructure is already connected via `AddControllers()` and `MapControllers()` or project equivalent.
- [ ] T052 Add or change the controller infrastructure configuration only if it does not exist; do not duplicate the global registration for one feature.
- [ ] T053 Create or update `<Feature>Controller` in `<exact path>` and define the route group according to the agreed API contract.
- [ ] T054 Add endpoint method for `<Get…ListQuery>`: accept HTTP parameters, map them to Mediator request, pass `CancellationToken` and return existing `ToActionResult` mapping.
- [ ] T055 Add endpoint method for `<Get…ByIdQuery>`: get route parameter, map it to Mediator request, pass `CancellationToken` and return existing `ToActionResult` mapping.
- [ ] T056 Add an endpoint method for `<Create…Command>`: accept a request body, map it to a command, pass it to the Mediator and return the existing `ToActionResult` mapping.
- [ ] T057 Add an endpoint method for `<Update…Command>`: match the route identifier and request body, map them to the command, pass it to the Mediator and return the existing `ToActionResult` mapping.
- [ ] T058 Add an endpoint method for `<Delete…Command>`: get the route identifier, map it to the command, pass it to the Mediator and return the existing `ToActionResult` mapping.
- [ ] T059 Add an endpoint method for each additional use case as a separate task: `<use case name>`; use the existing Result-to-HTTP mapping, and create a new one only when required by the agreed contract.

## Checkpoint

Controller infrastructure is connected only as needed; each agreed use case has a separate thin endpoint, and the controller only performs HTTP-to-Mediator mapping.
