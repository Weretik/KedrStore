# Template of subphase 01.NN — Domain: value object

- [ ] TNN1 Create or modify `<ValueObject>` to `<exact path>`; make it immutable according to the conventions of the module.
- [ ] TNN2 Implement normalization, equality and value validation within Domain.
- [ ] TNN3 [P] Add unit tests: equality, inequality, normalization, invalid values ​​and limit values.

## Checkpoint

Value object expresses an objective concept, has no persistence/API concerns and does not allow an incorrect state.
