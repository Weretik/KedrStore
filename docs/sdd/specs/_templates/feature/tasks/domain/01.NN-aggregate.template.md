# Template of subphase 01.NN — Domain: aggregate and invariants

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Domain

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] TNN1 Create or modify `<AggregateRoot>` to `<exact path>`; define a single point of entry for changing the aggregate.
- [ ] TNN2 Implement `<invariant/state/behavior>` via Domain model methods, without EF Core, HTTP or Application dependencies.
- [ ] TNN3 Restrict direct editing of state: private setters, encapsulated collections and allowed transition methods according to module conventions.
- [ ] TNN4 [P] Add unit-tests: allowed changes, violation of each invariant and boundary state transitions.

## Checkpoint

Aggregate root protects agreed invariants and Domain code is independent of Infrastructure or API.
