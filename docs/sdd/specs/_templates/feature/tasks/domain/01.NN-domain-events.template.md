# Template of subphase 01.NN — Domain: domain events

- **Task ID:** TS-<NNN>
- **Covers:** <SC-IDs, or —>
- **Depends on:** <task IDs or none>
- **Exact paths:** <implementation and test paths>
- **Test level:** Domain

## Evidence

- Focused test:
- Red command and expected behavioral failure:
- Green command and result:
- Refactor notes:
- Regression command and result:

## Mandatory execution order

1. **Red:** add and run the smallest focused test; confirm the expected behavioral failure.
2. **Green:** implement only enough production code to pass that test; rerun it.
3. **Refactor:** improve the code while the focused test remains Green.
4. **Regression:** run the affected suite.
5. **Evidence:** replace every pending evidence field with the actual commands and results.

The Work checklist defines scope and must be carried out through these gates.

## Work

- [ ] TNN1 Fix a side effect between aggregates that cannot be expressed within one aggregate; do not add an event without such a rule.
- [ ] TNN2 Create a `<DomainEvent>` in `<exact path>` and raise it with Domain behavior after a successful state change.
- [ ] TNN3 Create a handler in the Application for each reaction to the event; define transactionality, order and failure behavior.
- [ ] TNN4 [P] Add unit tests for raising the event and tests handler for the required reaction.

## Checkpoint

Domain event describes a fact that has already happened; Domain does not perform infrastructural side effects directly.
