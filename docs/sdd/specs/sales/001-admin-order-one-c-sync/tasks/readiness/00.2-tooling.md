# Subphase 00.2 — Readiness: discover CLI and generators

- [x] T005 Verified `dotnet ef --version`: Entity Framework Core CLI `10.0.2` is available.
- [x] T006 Validated command syntax against EF Core 10.0.2 help; execute the migration only in T050 after the model is implemented:
  `dotnet ef migrations add AddSalesOrdersAndOneCOrderSync --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext --output-dir Migrations`.
- [x] T007 Validated command syntax against EF Core 10.0.2 help; execute the inspection commands only after T050 creates the migration:
  `dotnet ef migrations has-pending-model-changes --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext` and
  `dotnet ef migrations script --idempotent --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext`.
- [x] T008 Recorded the empty-database migration command:
  `dotnet ef database update --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext`.
  Run it later only with an operator-provided isolated PostgreSQL test connection; never use a production connection.
- [x] T009 Recorded build and test commands: `dotnet restore KedrStore.sln`, `dotnet build KedrStore.sln --no-restore`, and `dotnet test KedrStore.sln --no-build`.
- [x] T010 Confirmed that no standalone OpenAPI validator/generator is documented. `tests/IntegrationTests/ApiContractTests.cs` contains the existing runtime checks `OpenApi_Document_Is_Available` and `OpenApi_Contains_Expected_Public_Endpoints`; extend that test for this endpoint in Phase 04.

## Checkpoint

EF Core commands and the existing runtime OpenAPI verification path are recorded. Database update remains explicitly limited to an isolated operator-provided test database.

## Verification record

- `dotnet ef --version` — succeeded; version `10.0.2`.
- `dotnet ef migrations add --help`, `has-pending-model-changes --help`, `migrations script --help`, and `database update --help` — succeeded; required options are supported.
- Repository inspection — no standalone OpenAPI CLI was found; runtime OpenAPI tests are in `tests/IntegrationTests/ApiContractTests.cs`.
- No migration, database update, build, or test was executed in this readiness subphase; those commands belong to their implementation/verification tasks.
