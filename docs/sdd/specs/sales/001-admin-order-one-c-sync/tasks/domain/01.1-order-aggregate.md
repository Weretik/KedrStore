# T010 — Domain: Order aggregate and lines

**Depends on:** T001–T006

- [x] Created `Order` in `src/Sales/Sales.Domain/Orders/Entities/Order.cs`; it owns technical `Id`, immutable `OrderNumber`, `CounterpartyId`, timestamp, comment, and a private line collection.
- [x] Created `OrderLine` in `src/Sales/Sales.Domain/Orders/Entities/OrderLine.cs`; it stores historical `ProductId`, `ProductName`, positive quantity, and total line amount.
- [x] Created expected invariant errors in `src/Sales/Sales.Domain/Orders/Errors/OrderErrors.cs`.
- [x] Added aggregate and line factories that validate data before an order can be created.
- [x] Made `CounterpartyId` and `OrderNumber` immutable after creation; empty lines, blank product fields, non-positive quantity, negative amount, and invalid order numbers are rejected.

## Checkpoint

The aggregate can represent a valid manager order without any EF Core, SOAP, HTTP, or Application type.
