# T020 — Domain: 1C order delivery lifecycle

**Depends on:** T010

- [x] Created `OneCOrderSync` and `OneCOrderSyncStatus` in `src/Sales/Sales.Domain/Orders/`.
- [x] Modelled `Pending`, `Sent`, `Accepted`, `BusinessError`, `TransportError`, `RetryScheduled`, and `DeadLetter` states.
- [x] Encapsulated attempt, accept, business error, transport retry/dead-letter, and sending-window deferral transitions.
- [x] Preserved technical `OrderId` as immutable 1:1 delivery key and prevented automatic attempts after terminal `Accepted`, `BusinessError`, or `DeadLetter` states.
- [x] Bounded persisted error/response diagnostics and accepted only a request payload hash, never a raw SOAP request payload.

## Checkpoint

Every delivery transition is explicit, testable, and cannot accidentally recreate a delivered 1C document.
