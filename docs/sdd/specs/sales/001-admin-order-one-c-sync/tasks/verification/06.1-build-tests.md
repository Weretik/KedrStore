# T170 — Verification: build, tests, migration, and job

**Depends on:** T030, T050, T080, T100, T140, T150, T160

- [ ] Run `dotnet restore KedrStore.sln`.
- [ ] Run `dotnet build KedrStore.sln --no-restore`.
- [ ] Run `dotnet test KedrStore.sln --no-build`.
- [ ] Run the exact EF migration creation, empty-database update, and pending-model-changes commands recorded in T050.
- [ ] Run the OpenAPI validation/generation command recorded in T130.
- [ ] Run `Host.Jobs` with `--job=sync-one-c-orders` against a controlled SOAP substitute or a non-production 1C endpoint; verify accepted, business-error, and transport-error state transitions.
- [ ] Record every command, result, environment limitation, and pre-existing failure.

## Checkpoint

Automated and controlled-runtime evidence covers compilation, tests, migration, API contract, and the dedicated order-sync job.
