# T170 — Verification: build, tests, migration, and job

**Depends on:** T030, T050, T080, T100, T140, T150, T160

**Status:** Complete — 2026-09-09.

- [x] Run `dotnet restore KedrStore.sln`.
- [x] Run `dotnet build KedrStore.sln --no-restore`.
- [x] Run `dotnet test KedrStore.sln --no-build`.
- [x] Run the exact EF migration creation, empty-database update, and pending-model-changes commands recorded in T050.
- [x] Run the OpenAPI validation/generation command recorded in T130.
- [x] Run `Host.Jobs` with `--job=sync-one-c-orders` against a controlled SOAP substitute or a non-production 1C endpoint; verify accepted, business-error, and transport-error state transitions.
- [x] Record every command, result, environment limitation, and pre-existing failure.

## Checkpoint

Automated and controlled-runtime evidence covers compilation, tests, migration, API contract, and the dedicated order-sync job.

## Verification record

- 2026-09-09 — final T170 verification after the manual-retry contract types were split into separate files: `dotnet restore KedrStore.sln` succeeded; `dotnet build KedrStore.sln --no-restore` succeeded with 0 errors; `dotnet test KedrStore.sln --no-build --no-restore` passed UnitTests 50/50, ArchitectureTests 10/10, and IntegrationTests 49/51 with 2 PostgreSQL Testcontainers tests skipped because Docker was unavailable locally. `dotnet ef database update ... --no-build` applied all Sales migrations through `20260909154836_AddOrderSyncRetryAudit`; `dotnet ef migrations has-pending-model-changes ... --no-build` reported no model changes. Existing `Microsoft.OpenApi` and `Scriban` vulnerability warnings remain recorded in the delivery report.
- 2026-09-09 — manual retry verification: build succeeded; EF reported no changes since migration `AddOrderSyncRetryAudit`; UnitTests 50 passed, ArchitectureTests 10 passed, IntegrationTests 49 passed and 2 PostgreSQL tests skipped locally because Docker is unavailable. The new retry persistence test is mandatory when `CI=true`.
- 2026-09-09 — latest `dotnet test KedrStore.sln --no-restore` run after adding exact batch-20, tenth-attempt dead-letter, and concurrent-worker coverage: UnitTests 65 passed; IntegrationTests 22 passed; 0 failed.
- 2026-09-09 — latest `dotnet test KedrStore.sln --no-restore` run after moving order-sync settings to configuration: UnitTests 62 passed; IntegrationTests 22 passed; 0 failed. Existing package vulnerability warnings for `Microsoft.OpenApi` and `Scriban` remain.

- Latest controlled `sync-one-c-orders` runtime verification (2026-09-07): a temporary SOAP substitute listened only on `127.0.0.1`, while the job used an isolated PostgreSQL schema. It could not access ordinary orders and no request reached 1C. The job exited with `[SUCCESS] Job finished OK`: `VERIFY-ACCEPTED` became `Accepted` with `AttemptCount=1` and `OneCDocumentNumber=MOCK-ACCEPTED-001`; `VERIFY-BUSINESS-ERROR` became `BusinessError` with no document number; `VERIFY-TRANSPORT-ERROR` became `RetryScheduled` with `AttemptCount=1`, `LastErrorCode=TransportError`, and its next attempt five minutes later. Summary: `Processed=3; Accepted=1; BusinessError=1; TransportError=1; Deferred=0; DeadLetter=0`.

- `dotnet restore KedrStore.sln` — succeeded; dependencies were already up to date. Eight package vulnerability warnings remain for `Microsoft.OpenApi` and `Scriban`.
- `dotnet build KedrStore.sln --no-restore` — succeeded.
- `dotnet test KedrStore.sln --no-build` — IntegrationTests: 22 passed. UnitTests: 53 passed, 1 failed: `UnitTests.SlugTests.MapProduct_Should_Clean_Slugs` at `tests/UnitTests/SlugTests.cs:33`; this failure was unrelated to Sales order synchronization.
- 2026-09-09 — `dotnet test KedrStore.sln --no-restore` succeeded after the slug normalization, dead-letter notification retry, and stale-`Sent` recovery fixes: UnitTests 58 passed; IntegrationTests 22 passed; 0 failed. Package vulnerability warnings for `Microsoft.OpenApi` and `Scriban` remain recorded as residual dependency risk.
- `dotnet ef migrations has-pending-model-changes --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext` — succeeded: no pending model changes.
- `dotnet ef database update --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext` — succeeded on the approved local test PostgreSQL database. `dotnet ef migrations list ...` then showed `20260902083749_AddDeadLetterTelegramNotificationRetry` and `20260907111114_PersistOneCDocumentNumber` applied.
- `DOTNET_ENVIRONMENT=Development dotnet run --project src/Bootstrapper/Host.Jobs/Host.Jobs/Host.Jobs.csproj -- --job=migrate` — succeeded. Catalog, Sales, and Identity migrations completed; RoleSeeder completed. IdentitySeeder skipped default-admin creation because `ADMIN_DEFAULT_PASSWORD` is absent from local secrets.
- `dotnet test tests/IntegrationTests/IntegrationTests.csproj --no-build --filter FullyQualifiedName~ApiContractTests` — passed: 3 tests. No standalone OpenAPI validation/generation command is recorded in T130.
