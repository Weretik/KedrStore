# T110 — API: admin orders controller

**Depends on:** T100

- [x] Create `src/Sales/Sales.Api/Controllers/AdminOrdersController.cs` with route `api/admin/orders`.
- [x] Add `POST` endpoint that binds the agreed API request, sends `CreateOrderCommand` through `ISender`, passes `CancellationToken`, and returns the existing `ToActionResult` mapping.
- [x] Keep controller logic limited to HTTP-to-command mapping and response metadata; no EF, counterparty lookup, or SOAP logic.

## Checkpoint

The endpoint is a thin adapter to the Application use case.
