# T140 — API: integration tests

**Depends on:** T120, T130

- [x] Add tests under `tests/IntegrationTests/` for successful manager/admin creation, validation failure, unknown/deleted counterparty, and idempotency replay/conflict. Add unauthenticated and unauthorized scenarios when the production follow-up enables `PolicyNames.CanManageOrders`.
- [x] Assert HTTP status, response schema, `Pending` status, and absence of a synchronous SOAP call.
- [x] Check the test contract against `docs/sdd/contracts/sales/admin-order-one-c-sync.openapi.yaml`.

## Checkpoint

HTTP behaviour, authorization, and the durable asynchronous boundary are demonstrated by integration tests.

## Verification

- 2026-09-02 — `dotnet test tests/IntegrationTests/IntegrationTests.csproj --no-restore --filter FullyQualifiedName~AdminOrderApiTests`: passed, 6 passed / 0 failed. Verified `201 Created` for initial creation, `200 OK` for an idempotent replay, contract response schema (`syncStatus: "Pending"`), validation/not-found/conflict behavior, and no synchronous 1C write.
