# T140 — API: integration tests

**Depends on:** T120, T130

- [ ] Add tests under `tests/IntegrationTests/` for successful manager/admin creation, validation failure, unknown/deleted counterparty, unauthenticated request, unauthorized user, and idempotency replay/conflict.
- [ ] Assert HTTP status, response schema, `Pending` status, and absence of a synchronous SOAP call.
- [ ] Check the test contract against `docs/sdd/contracts/sales/admin-order-one-c-sync.openapi.yaml`.

## Checkpoint

HTTP behaviour, authorization, and the durable asynchronous boundary are demonstrated by integration tests.
