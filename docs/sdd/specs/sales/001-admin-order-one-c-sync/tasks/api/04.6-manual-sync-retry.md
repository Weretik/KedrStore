# T136 — Audited manual retry API

**Depends on:** T100, T135, T150

- [x] Add `POST /api/admin/orders/{orderId}/sync/retry` with a required bounded reason.
- [x] Allow only `BusinessError` and `DeadLetter`; return conflict for every other synchronization status.
- [x] Persist the previous status, attempt count, safe diagnostics, reason, caller, and time in `OrderSyncRetryAudits`.
- [x] Reset the delivery and notification cycle, transition to due `RetryScheduled`, and reuse the existing `OrderNumber`.
- [x] Keep SOAP delivery outside the HTTP request; the existing Job performs the send.
- [x] Use PostgreSQL `xmin` to reject concurrent retry transitions.
- [x] Update OpenAPI, runbook, data model, requirements, and infrastructure design.
- [x] Cover domain, Application, API, model/migration, and PostgreSQL persistence behavior.

## Checkpoint

A valid request returns `202 Accepted` only after the audit row and `RetryScheduled` state are saved atomically. It never means that 1C has already accepted the order.

## Verification record

- 2026-09-09 — Red: the first endpoint test returned `401 Unauthorized` because the retry route did not exist.
- 2026-09-09 — Green: focused domain, Application, API, and migration-model tests passed. The PostgreSQL runtime test was added and discovered; it is skipped locally because Docker is unavailable and is mandatory in CI.
