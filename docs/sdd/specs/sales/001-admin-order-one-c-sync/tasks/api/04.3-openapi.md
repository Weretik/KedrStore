# T130 — API: OpenAPI contract

**Depends on:** T120

- [x] Create `docs/sdd/contracts/sales/admin-order-one-c-sync.openapi.yaml` for `POST /api/admin/orders`.
- [x] Define request/response schemas, required `Idempotency-Key`, temporary anonymous access, status codes, validation/error schema, and examples. Record Manager/Admin authorization as the required production follow-up.
- [x] Add the contract by `$ref` to `docs/sdd/contracts/openapi.yaml` and update `contracts/api-contract.md` to match the implemented endpoint.

## Checkpoint

The versioned contract is the API handoff source and matches the endpoint.
