# T130 — API: OpenAPI contract

**Depends on:** T120

- [ ] Create `docs/sdd/contracts/sales/admin-order-one-c-sync.openapi.yaml` for `POST /api/admin/orders`.
- [ ] Define request/response schemas, required `Idempotency-Key`, temporary anonymous access, status codes, validation/error schema, and examples. Record Manager/Admin authorization as the required production follow-up.
- [ ] Add the contract by `$ref` to `docs/sdd/contracts/openapi.yaml` and update `contracts/api-contract.md` to match the implemented endpoint.
- [ ] Run the repository's available OpenAPI validation/generation command and record it in T170.

## Checkpoint

The versioned contract is the API handoff source and matches the endpoint.
