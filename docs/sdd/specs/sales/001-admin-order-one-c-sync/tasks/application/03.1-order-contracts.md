# T090 — Application: order contracts

**Depends on:** T010, T020

- [x] Create `src/Sales/Sales.Application/Features/Orders/Create/` and define `CreateOrderCommand`, request/line DTOs, result DTO, and validator. `CreateOrderCommandHandler` implementation is owned by T100.
- [x] Define an Application abstraction for a database-backed `SO-YYYYMMDD-NNNN` order-number generator; Application passes the generated number to `Order.Create`.
- [x] Add only required Application-facing abstractions for counterparty lookup, transactional order persistence, and the durable synchronization record. Keep `SalesDbContext` out of Application.
- [x] Reuse the `ISalesOneCWriteClient` request/result DTOs introduced by T060; they must not expose generated WCF classes.
- [x] Reuse existing `IReadSalesDbContext` or `ISalesRepository<T>` where suitable; document any new abstraction's single consumer. `IOrderNumberGenerator`, `IOrderProductReader`, and `IOrderCreationStore` are consumed only by T100's `CreateOrderCommandHandler`; the existing read context and generic repository cannot atomically persist the Infrastructure-only idempotency record.

## Checkpoint

Application has explicit CQRS and integration contracts without an Infrastructure dependency.
