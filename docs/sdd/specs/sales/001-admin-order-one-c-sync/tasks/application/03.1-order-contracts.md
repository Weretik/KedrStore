# T090 — Application: order contracts

**Depends on:** T010, T020

- [ ] Create `src/Sales/Sales.Application/Features/Orders/Create/` and define `CreateOrderCommand`, request/line DTOs, result DTO, validator, and handler.
- [ ] Define an Application abstraction for a database-backed `SO-YYYYMMDD-NNNN` order-number generator; Application passes the generated number to `Order.Create`.
- [ ] Add only required Application-facing abstractions for counterparty lookup, transactional order persistence, and the durable synchronization record. Keep `SalesDbContext` out of Application.
- [ ] Reuse the `ISalesOneCWriteClient` request/result DTOs introduced by T060; they must not expose generated WCF classes.
- [ ] Reuse existing `IReadSalesDbContext` or `ISalesRepository<T>` where suitable; document any new abstraction's single consumer.

## Checkpoint

Application has explicit CQRS and integration contracts without an Infrastructure dependency.
