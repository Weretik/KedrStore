# T060 — Infrastructure: 1C SOAP write adapter

**Depends on:** T040

- [ ] Add the required Application abstraction at `src/Sales/Sales.Application/Integrations/OneC/Contracts/ISalesOneCWriteClient.cs` and request/result DTOs under `DTOs/`. Infrastructure owns the adapter implementation, while the interface remains Application-owned to preserve dependency direction.
- [ ] Implement it as `src/Sales/Sales.Infrastructure/Integrations/OneC/SalesOneCWriteClient.cs`, using `OneCSoapClientFactory` and the generated `CreateSiteRequestAsync` operation.
- [ ] Map `CounterpartyId`, stable human-readable `OrderNumber` to `RequestData.OrderId`, date, comment, and every order line to `RequestData`/`Item` only inside Infrastructure. Do not edit `BuildingBlocks.Integrations.OneC/Generated/Reference.cs`.
- [ ] Return an Application result which differentiates accepted response, explicit 1C business rejection, and transport exception. Apply the agreed `RequestDataOut.DocId`/`Comment` interpretation.
- [ ] Register the client in `src/Sales/Sales.Infrastructure/DependencyInjection/SalesInfrastructureExtensions.cs`.
- [ ] Log only structured delivery identifiers and sanitized bounded response diagnostics; never log Basic-auth credentials, headers, or raw request data.

## Checkpoint

SOAP transport is isolated behind a Sales Application contract and has deterministic accepted/business/transport outcomes.
