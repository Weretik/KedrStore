# T040 — Infrastructure: Sales order persistence

**Depends on:** T010, T020

- [ ] Add `DbSet<Order>`, `DbSet<OrderLine>`, `DbSet<OneCOrderSync>`, and the Infrastructure-only `DbSet<OrderIdempotencyRecord>` to `src/Sales/Sales.Infrastructure/DataBase/SalesDbContext.cs`.
- [ ] Add `OrderConfiguration`, `OrderLineConfiguration`, `OneCOrderSyncConfiguration`, and `OrderIdempotencyRecordConfiguration` under `src/Sales/Sales.Infrastructure/Configurations/`, including EF value converters for typed Domain IDs to `bigint`.
- [ ] Map the private order-line collection, unique immutable `OrderNumber`, the required counterparty relation, one-to-one unique delivery record, idempotency key/request hash/expiry with operation-scoped unique index, string lengths, decimal precision, state conversion, and `Status + NextAttemptAtUtc` due-record index.
- [ ] Define the database-level concurrency/claim mechanism for job workers before implementation: optimistic concurrency token or provider-safe atomic claim query.
- [ ] Extend `ISalesRepository<T>` only if the existing repository cannot persist the aggregate and query due delivery records without leaking EF Core into Application.

## Checkpoint

Mappings support the domain model and enforce one delivery lifecycle per order without putting persistence attributes in Domain.
