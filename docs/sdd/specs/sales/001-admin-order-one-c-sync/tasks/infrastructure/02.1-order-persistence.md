# T040 — Infrastructure: Sales order persistence

**Depends on:** T010, T020

- [x] Add `DbSet<Order>`, `DbSet<OrderLine>`, `DbSet<OneCOrderSync>`, and the Infrastructure-only `DbSet<OrderIdempotencyRecord>` to `src/Sales/Sales.Infrastructure/DataBase/SalesDbContext.cs`.
- [x] Add `OrderConfiguration`, `OrderLineConfiguration`, `OneCOrderSyncConfiguration`, and `OrderIdempotencyRecordConfiguration` under `src/Sales/Sales.Infrastructure/Configurations/`, including EF value converters for typed Domain IDs to `bigint`.
- [x] Map the private order-line collection, unique immutable `OrderNumber`, the required counterparty relation, one-to-one unique delivery record, idempotency key/request hash/expiry with operation-scoped unique index, string lengths, decimal precision, state conversion, and `Status + NextAttemptAtUtc` due-record index.
- [x] Define the database-level concurrency/claim mechanism for job workers before implementation: optimistic concurrency token or provider-safe atomic claim query.
- [x] Extend `ISalesRepository<T>` only if the existing repository cannot persist the aggregate and query due delivery records without leaking EF Core into Application. The existing Ardalis generic repository supports aggregate persistence and specification-based due-record queries, so no extension is needed.

## Checkpoint

Mappings support the domain model and enforce one delivery lifecycle per order without putting persistence attributes in Domain.

The Catalog registration was corrected by aligning `DbContextOptions<CatalogDbContext>` with the singleton `IDbContextFactory<CatalogDbContext>`; the temporary Sales design-time factory was removed. The regular Host.Api EF CLI path now succeeds in T050.

## Verification record

- `dotnet build src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --no-restore` — succeeded with 0 errors. Existing NU1903 dependency-vulnerability warnings remain.
- `dotnet ef dbcontext info --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext --no-build` — blocked before creating `SalesDbContext` by pre-existing `Host.Api` service validation: singleton `IDbContextFactory<CatalogDbContext>` consumes scoped `DbContextOptions<CatalogDbContext>`. Resolve this or provide a `SalesDbContext` design-time factory before T050 generates the migration.
