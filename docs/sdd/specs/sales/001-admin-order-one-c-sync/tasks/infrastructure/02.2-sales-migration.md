# T050 — Infrastructure: Sales database migration

**Depends on:** T040

- [ ] Generate the migration through EF CLI for `SalesDbContext`; do not modify `20260529143043_InitialSalesDbContext` or its snapshot manually.
- [ ] Use `src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj` as the project and `src/Bootstrapper/Host.Api/Host.Api.csproj` as the startup project; record the exact command actually accepted by this solution.
- [ ] Inspect the generated migration for destructive operations, schema locks, numeric precision, unique order-sync constraint, and due-record index.
- [ ] Apply it to an empty PostgreSQL-compatible test database and run the EF pending-model-changes command when supported.
- [ ] Record rollout: schema first, then API/job deployment, then scheduler enablement. Record rollback: disable job and roll back application while retaining new business tables.

## Checkpoint

The generated migration has been reviewed and its deployment/recovery behaviour is documented.
