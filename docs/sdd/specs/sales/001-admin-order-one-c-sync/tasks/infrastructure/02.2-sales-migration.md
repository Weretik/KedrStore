# T050 — Infrastructure: Sales database migration

**Depends on:** T040

- [x] Generate the migration through EF CLI for `SalesDbContext`; do not modify `20260529143043_InitialSalesDbContext` or its snapshot manually.
- [x] Use `src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj` as the project and `src/Bootstrapper/Host.Api/Host.Api.csproj` as the startup project; record the exact command actually accepted by this solution.
- [x] Inspect the generated migration for destructive operations, schema locks, numeric precision, unique order-sync constraint, and due-record index.
- [x] Apply it to an empty PostgreSQL-compatible test database and run the EF pending-model-changes command when supported.
- [x] Record rollout: schema first, then API/job deployment, then scheduler enablement. Record rollback: disable job and roll back application while retaining new business tables.

## Checkpoint

The generated migration has been reviewed and its deployment/recovery behaviour is documented.

EF CLI now creates `SalesDbContext` through the standard Host.Api service provider; no Sales-specific design-time factory remains.

## Verification record

- Generated `20260817214830_AddSalesOrdersAndOneCOrderSync` with:
  `dotnet ef migrations add AddSalesOrdersAndOneCOrderSync --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext --output-dir Migrations`.
  EF CLI now uses the standard `Host.Api` design-time path after Catalog's `DbContextOptions` lifetime was aligned with its singleton `IDbContextFactory`.
- Reviewed the generated `Up`: it contains only new Sales tables, foreign keys and indexes; no destructive operation. `Amount` is `numeric(18,2)`; `OneCOrderSyncs.OrderId` is unique; `IX_OneCOrderSyncs_Status_NextAttemptAtUtc` supports due-record selection. Creating these new tables/indexes takes normal DDL locks only; there is no lock on an existing business table.
- `dotnet ef migrations has-pending-model-changes --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext` — succeeded: no model changes since the migration.
- Applied `dotnet ef database update --project src/Sales/Sales.Infrastructure/Sales.Infrastructure.csproj --startup-project src/Bootstrapper/Host.Api/Host.Api.csproj --context SalesDbContext` to the user-approved local PostgreSQL database. `dotnet ef migrations list` confirms `20260817214830_AddSalesOrdersAndOneCOrderSync` is applied.
- Rollout: deploy and apply schema first; then deploy API and job code; then enable the scheduler in its configured Kyiv window. Rollback: disable the job and roll back the application while retaining the new order, delivery, and idempotency tables. Do not run this migration's generated `Down` after business records may exist.
