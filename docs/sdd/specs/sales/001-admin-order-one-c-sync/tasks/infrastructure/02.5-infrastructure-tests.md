# T080 — Infrastructure: integration tests

**Depends on:** T040, T050, T060

- [ ] Add persistence and migration tests under `tests/IntegrationTests/` for the unique order-sync relation, due-record query/index behaviour, and counterparty relationship.
- [ ] Add SOAP adapter tests with a controllable test double/endpoint for request mapping, accepted response, 1C business response, timeout/network failure, and no sensitive log data.
- [ ] Run the targeted IntegrationTests project and record the outcome.

## Checkpoint

Critical persistence and SOAP adapter behaviour is verified outside only Domain test doubles. Background-job tests are completed in T150.
